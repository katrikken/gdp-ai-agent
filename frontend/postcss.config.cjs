module.exports = {
  plugins: {
    // 1. Tailwind CSS processes its own directives first
    tailwindcss: {},
    // 2. Autoprefixer adds vendor prefixes for browser compatibility
    autoprefixer: {},
  },
};